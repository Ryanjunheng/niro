<%= form_for Reporting.new, url: reporting_user_path, method: :post do |f| %>
<%= f.hidden_field :reported_id, value:@user.id %>
<%= f.hidden_field :reported_type, value:"User" %>
<%= f.hidden_field :reporter_id, value:current_user.id %>
<div class="form-group">
  <%= f.text_area :comment, class:"form-control", rows:6 %>
</div>
<div class="form-group">
  <%= f.submit "Submit", class:"btn btn-primary pull-right" %>
</div>
<% end %>
